# @package _global_
defaults:
  - override /model: transformer  
  - override /tracker: development_tracker
  - override /optimizer: adamw_muon
  
name: shapenet-car-transformer-float16

trainer:
  precision: float16