# @package _global_
defaults:
  - override /model: transformer_completep_base
  - override /tracker: development_tracker
  - override /optimizer: adamw_completep_base

name: shapenet-transformer-completep-base
