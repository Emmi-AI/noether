# @package _global_
defaults:
  - override /model: transformer_completep_large
  - override /tracker: development_tracker
  - override /optimizer: adamw_completep_large

name: shapenet-transformer-completep-large
