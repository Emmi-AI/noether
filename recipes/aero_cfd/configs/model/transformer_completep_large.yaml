kind: noether.modeling.models.AeroTransformer
name: transformer-completep-large
hidden_dim: 512
depth: 16
transformer_block_config:
  num_heads: 8
  mlp_expansion_factor: 4
  use_rope: true
completep_config:
  base_width: 256
  base_depth: 2
  depth_alpha_exp: 1.0
  init_std: 0.02
  output_alpha: 1.0
optimizer_config: ${optimizer}
data_specs: ${data_specs}
forward_properties:
  - surface_position
  - volume_position
  - surface_features
  - volume_features
